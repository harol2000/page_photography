<section class="hero">
  <div class="hero-media">
    <video class="hero-video active" autoplay muted loop playsinline preload="auto">
      <source src="https://harol2000.github.io/page_photography/videos/VIDEO_PORTADA_720.mp4" type="video/mp4" />
    </video>
  </div>

  <div class="hero-logo-wrap">
    <img
      src="https://harol2000.github.io/page_photography/images/LOGO_FRANKTUCNO_NEGATIVO.png"
      alt="Studio27"
      class="hero-logo-center"
    />
  </div>

  <button class="hero-arrow hero-prev" aria-label="Video anterior">‹</button>
  <button class="hero-arrow hero-next" aria-label="Video siguiente">›</button>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    overflow: hidden;
    background: #000;
  }

  .hero-media {
    position: absolute;
    inset: 0;
  }

  .hero-video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .hero-logo-wrap {
    position: absolute;
    inset: 0;
    z-index: 3;
    display: grid;
    place-items: center;
    pointer-events: none;
  }

  .hero-logo-center {
    width: min(520px, 70vw);
    height: auto;
    display: block;
    object-fit: contain;
  }

  .hero-arrow {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    z-index: 4;
    width: 54px;
    height: 54px;
    border-radius: 999px;
    border: 1px solid rgba(255,255,255,.7);
    background: rgba(0,0,0,.18);
    color: white;
    font-size: 2rem;
    line-height: 1;
    cursor: pointer;
    backdrop-filter: blur(6px);
    transition: background .25s ease;
  }

  .hero-arrow:hover {
    background: rgba(0,0,0,.35);
  }

  .hero-prev {
    left: 22px;
  }

  .hero-next {
    right: 22px;
  }

  @media (max-width: 768px) {
    .hero {
      min-height: 72vh;
    }

    .hero-logo-center {
      width: min(320px, 72vw);
    }

    .hero-arrow {
      width: 44px;
      height: 44px;
      font-size: 1.6rem;
    }

    .hero-prev {
      left: 12px;
    }

    .hero-next {
      right: 12px;
    }
  }
</style>
